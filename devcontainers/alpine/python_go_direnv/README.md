# Alpine Python + Go + Direnv

Devcontainer using an Alpine base with Python, Go, and Direnv.

Builds a [Dockerfile](./Dockerfile) and copies a pre-configured [`.bashrc`](./dot_bashrc) into the container, enabling Bash tab completions & auto-sourcing your `.envrc`.
